let id = 0
class Auditorium {
    constructor(room_number, type, seats_av, total_capacity) {  
            this.room_number = room_number
            this.type = type
            this.seats_av = seats_av
            this.total_capacity = total_capacity
    }
}

module.exports = Auditorium
